<UserControl x:Class="AhuErp.UI.Views.SubstitutionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AhuErp.UI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:SubstitutionsViewModel}"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Замещения сотрудников"
                   FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12" />

        <Border Grid.Row="1" Padding="12" Background="White" CornerRadius="4" Margin="0,0,0,8">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="Замещаемый:" Width="120" VerticalAlignment="Center" />
                    <ComboBox ItemsSource="{Binding Employees}"
                              SelectedItem="{Binding SelectedOriginal}"
                              DisplayMemberPath="FullName"
                              MinWidth="220" Padding="6" Margin="0,0,12,0" />
                    <TextBlock Text="Заместитель:" Width="120" VerticalAlignment="Center" />
                    <ComboBox ItemsSource="{Binding Employees}"
                              SelectedItem="{Binding SelectedSubstitute}"
                              DisplayMemberPath="FullName"
                              MinWidth="220" Padding="6" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="С:" Width="120" VerticalAlignment="Center" />
                    <DatePicker SelectedDate="{Binding FromDate}" Width="120" Margin="0,0,12,0" />
                    <TextBlock Text="По:" Width="40" VerticalAlignment="Center" />
                    <DatePicker SelectedDate="{Binding ToDate}" Width="120" Margin="0,0,12,0" />
                    <TextBlock Text="Область:" Width="80" VerticalAlignment="Center" />
                    <ComboBox ItemsSource="{Binding Scopes}" SelectedItem="{Binding Scope}" MinWidth="160" Padding="6" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="Причина:" Width="120" VerticalAlignment="Center" />
                    <TextBox Text="{Binding Reason, UpdateSourceTrigger=PropertyChanged}" Width="500" Padding="6" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Content="Создать замещение" Command="{Binding CreateCommand}" Padding="10,4" Margin="0,0,12,0" />
                    <CheckBox Content="Только активные" IsChecked="{Binding ShowActiveOnly}" VerticalAlignment="Center" Margin="0,0,12,0" />
                    <Button Content="Обновить" Command="{Binding ReloadCommand}" Padding="10,4" />
                </StackPanel>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,6,0,0" />
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="60" />
                <DataGridTextColumn Header="Замещаемый" Binding="{Binding OriginalEmployee.FullName}" Width="*" />
                <DataGridTextColumn Header="Заместитель" Binding="{Binding SubstituteEmployee.FullName}" Width="*" />
                <DataGridTextColumn Header="С" Binding="{Binding From, StringFormat=dd.MM.yyyy}" Width="100" />
                <DataGridTextColumn Header="По" Binding="{Binding To, StringFormat=dd.MM.yyyy}" Width="100" />
                <DataGridTextColumn Header="Область" Binding="{Binding Scope, Converter={StaticResource EnumDisplayConverter}}" Width="180" />
                <DataGridCheckBoxColumn Header="Активно" Binding="{Binding IsActive}" Width="80" />
                <DataGridTextColumn Header="Причина" Binding="{Binding Reason}" Width="*" />
                <DataGridTemplateColumn Header="Действия" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Отменить"
                                    Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Padding="6,2" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Margin="0,8,0,0"
                   Text="Замещение перенаправляет новые поручения и/или согласования заместителю на период действия."
                   Foreground="Gray" TextWrapping="Wrap" />
    </Grid>
</UserControl>
