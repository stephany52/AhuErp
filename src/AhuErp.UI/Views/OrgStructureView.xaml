<UserControl x:Class="AhuErp.UI.Views.OrgStructureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AhuErp.UI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:OrgStructureViewModel}"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Оргструктура"
                   FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12" />

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <Button Content="Обновить" Command="{Binding ReloadCommand}" Padding="10,4" Margin="0,0,12,0" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" VerticalAlignment="Center" TextWrapping="Wrap" />
        </StackPanel>

        <Border Grid.Row="2" Background="#F9FAFB" CornerRadius="4" Padding="8" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Назначить руководителя выбранному отделу:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox ItemsSource="{Binding Employees}"
                          SelectedItem="{Binding NewHead}"
                          DisplayMemberPath="FullName"
                          IsEditable="True"
                          IsTextSearchEnabled="True"
                          TextSearch.TextPath="FullName"
                          MinWidth="260" Padding="6" Margin="0,0,8,0" />
                <Button Content="Назначить"
                        Command="{Binding AssignHeadCommand}"
                        Padding="10,4" />
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Background="White" Padding="12" CornerRadius="4">
            <TreeView ItemsSource="{Binding Roots}"
                      SelectedValuePath="Id"
                      SelectedItemChanged="TreeView_SelectedItemChanged">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type vm:DepartmentNode}"
                                              ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                            <TextBlock Text=" — " Margin="4,0,4,0" />
                            <TextBlock Text="{Binding ShortCode}" Foreground="Gray" />
                            <TextBlock Text="  Руководитель: " Margin="12,0,0,0" Foreground="Gray" />
                            <TextBlock Text="{Binding HeadName}" />
                            <TextBlock Text="  Сотрудников: " Margin="12,0,0,0" Foreground="Gray" />
                            <TextBlock Text="{Binding EmployeeCount}" />
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
        </Border>
    </Grid>
</UserControl>
