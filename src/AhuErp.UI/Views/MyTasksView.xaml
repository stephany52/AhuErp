<UserControl x:Class="AhuErp.UI.Views.MyTasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AhuErp.UI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MyTasksViewModel}"
             d:DesignHeight="600" d:DesignWidth="980">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Мои задачи / На контроле" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
            <TextBlock Text="Роль:" Margin="24,0,8,0" VerticalAlignment="Center" />
            <ComboBox ItemsSource="{Binding AvailableScopes}"
                      SelectedItem="{Binding SelectedScope}"
                      ItemTemplate="{StaticResource EnumComboBoxItemTemplate}"
                      MinWidth="180" Padding="6" />
            <Button Content="Обновить" Command="{Binding ReloadCommand}" Margin="12,0,0,0" Padding="10,4" />
        </StackPanel>

        <Border Grid.Row="1"
                Background="#FEE2E2" BorderBrush="#DC2626" BorderThickness="1"
                CornerRadius="4" Padding="10,6" Margin="0,0,0,8"
                Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#991B1B" TextWrapping="Wrap" />
        </Border>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Tasks}"
                  SelectedItem="{Binding SelectedTask, UpdateSourceTrigger=PropertyChanged}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowBackground="Transparent">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding Id}" Width="50" />
                <DataGridTextColumn Header="Документ" Binding="{Binding DocumentId}" Width="80" />
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*" />
                <DataGridTextColumn Header="Срок" Binding="{Binding Deadline, StringFormat=dd.MM.yyyy}" Width="100" />
                <DataGridTextColumn Header="Статус" Binding="{Binding Status, Converter={StaticResource EnumDisplayConverter}}" Width="140" />
                <DataGridTextColumn Header="Завершено" Binding="{Binding CompletedAt, StringFormat=dd.MM.yyyy}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Margin="0,12,0,0" Padding="12" Background="White" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Комментарий к решению" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox Text="{Binding ReportText, UpdateSourceTrigger=PropertyChanged}"
                             Padding="6"
                             ToolTip="Укажите результат исполнения или причину изменения статуса"
                             AcceptsReturn="True" TextWrapping="Wrap"
                             Height="60" VerticalScrollBarVisibility="Auto" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding SelectedTaskInfo}" Foreground="#4B5563" Margin="0,0,0,8" />
                    <StackPanel Orientation="Horizontal">
                        <Button Content="▶ В работу" Command="{Binding MarkInProgressCommand}" MinWidth="110" Padding="8,4" Margin="0,0,6,0"
                                ToolTip="Перевести выбранное поручение в работу" />
                        <Button Content="✓ Выполнено" Command="{Binding MarkCompletedCommand}" MinWidth="120" Padding="8,4"
                                ToolTip="Закрыть выбранное поручение как выполненное" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
