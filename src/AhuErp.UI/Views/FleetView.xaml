<UserControl x:Class="AhuErp.UI.Views.FleetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AhuErp.UI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:FleetViewModel}"
             d:DesignHeight="620" d:DesignWidth="1080">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Транспортное обслуживание" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Section 1: Vehicles -->
            <DockPanel Grid.Column="0">
                <TextBlock Text="Транспортные средства"
                           FontWeight="SemiBold" DockPanel.Dock="Top" Margin="0,0,0,6" />
                <Expander Header="Добавить ТС" DockPanel.Dock="Top" Margin="0,0,0,8">
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,6,6">
                            <TextBlock Text="Госномер" />
                            <TextBox Text="{Binding NewVehicleLicensePlate, UpdateSourceTrigger=PropertyChanged}" Padding="6" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,0,6">
                            <TextBlock Text="Модель" />
                            <TextBox Text="{Binding NewVehicleModel, UpdateSourceTrigger=PropertyChanged}" Padding="6" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,6,6">
                            <TextBlock Text="Статус" />
                            <ComboBox ItemsSource="{Binding VehicleStatuses}"
                                      SelectedItem="{Binding NewVehicleStatus}"
                                      ItemTemplate="{StaticResource EnumComboBoxItemTemplate}"
                                      Padding="6" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,6">
                            <TextBlock Text="Водитель по умолчанию" />
                            <TextBox Text="{Binding NewVehicleDriverName, UpdateSourceTrigger=PropertyChanged}" Padding="6" />
                        </StackPanel>
                        <Button Grid.Row="2" Grid.ColumnSpan="2" Content="Добавить ТС"
                                Command="{Binding AddVehicleCommand}" Padding="8,6" />
                    </Grid>
                </Expander>
                <DataGrid ItemsSource="{Binding Vehicles}"
                          SelectedItem="{Binding SelectedVehicle, UpdateSourceTrigger=PropertyChanged}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="№" Binding="{Binding Id}" Width="40" />
                        <DataGridTextColumn Header="Госномер" Binding="{Binding LicensePlate}" Width="110" />
                        <DataGridTextColumn Header="Модель" Binding="{Binding Model}" Width="*" />
                        <DataGridTextColumn Header="Статус" Binding="{Binding CurrentStatus, Converter={StaticResource EnumDisplayConverter}}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>

            <!-- Section 2: Trips -->
            <DockPanel Grid.Column="2">
                <TextBlock DockPanel.Dock="Top" Margin="0,0,0,6" FontWeight="SemiBold">
                    <Run Text="Поездки ТС" />
                    <Run Text="{Binding SelectedVehicle.LicensePlate, FallbackValue=—}" />
                </TextBlock>
                <DataGrid ItemsSource="{Binding SelectedVehicleTrips}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Начало"      Binding="{Binding StartDate, StringFormat=dd.MM.yyyy HH:mm}" Width="130" />
                        <DataGridTextColumn Header="Окончание"   Binding="{Binding EndDate, StringFormat=dd.MM.yyyy HH:mm}"   Width="130" />
                        <DataGridTextColumn Header="Водитель"    Binding="{Binding DriverName}" Width="*" />
                        <DataGridTextColumn Header="Документ № " Binding="{Binding DocumentId}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Grid>

        <!-- Section 3: Booking form -->
        <Border Grid.Row="2" Margin="0,16,0,0" Padding="16" Background="White" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Начало" />
                    <DatePicker SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock Text="Окончание" />
                    <DatePicker SelectedDate="{Binding EndDate, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,12,0">
                    <TextBlock Text="Водитель" />
                    <TextBox Text="{Binding DriverName, UpdateSourceTrigger=PropertyChanged}" Padding="6" />
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="0,0,12,0">
                    <TextBlock Text="Документ-основание" />
                    <ComboBox ItemsSource="{Binding TransportRequests}"
                              SelectedItem="{Binding SelectedDocument, UpdateSourceTrigger=PropertyChanged}"
                              DisplayMemberPath="Title"
                              Padding="6" />
                </StackPanel>

                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="Забронировать" Command="{Binding BookCommand}"    MinWidth="140" Padding="10,6" Margin="0,0,6,0" />
                    <Button Content="Обновить"       Command="{Binding RefreshCommand}" MinWidth="100" Padding="10,6" />
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.ColumnSpan="5"
                           Text="{Binding ErrorMessage}"
                           Foreground="#DC2626" TextWrapping="Wrap" Margin="0,10,0,0"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />
                <TextBlock Grid.Row="1" Grid.ColumnSpan="5"
                           Text="{Binding StatusMessage}"
                           Foreground="#047857" TextWrapping="Wrap" Margin="0,10,0,0"
                           Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
