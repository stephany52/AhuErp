<UserControl x:Class="AhuErp.UI.Views.ProcurementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AhuErp.UI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ProcurementViewModel}"
             d:DesignHeight="640" d:DesignWidth="1100">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Закупки 44-ФЗ" FontSize="20" FontWeight="SemiBold" />
            <Button Content="Обновить" Margin="16,0,0,0" Padding="12,4"
                    Command="{Binding RefreshCommand}" />
        </StackPanel>

        <TextBlock Grid.Row="1" Margin="0,8,0,16"
                   Foreground="#6B7280"
                   TextWrapping="Wrap">
            <Run Text="Phase 20 / Improvement #13 — карточный stub раздела." />
            <LineBreak />
            <Run Text="Полнофункциональный экран (план-график, процедуры, контракты, этапы) — следующая итерация." />
        </TextBlock>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Планы-графики"
                       FontWeight="SemiBold" Margin="0,0,0,8" />
            <DataGrid Grid.Row="1" Grid.Column="0"
                      ItemsSource="{Binding Plans}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Год" Binding="{Binding Year}" Width="80" />
                    <DataGridTextColumn Header="Наименование" Binding="{Binding Title}" Width="*" />
                    <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="120" />
                    <DataGridTextColumn Header="№ в ЕИС" Binding="{Binding EisRegistrationNumber}" Width="160" />
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Grid.Row="0" Grid.Column="2" Text="Контракты"
                       FontWeight="SemiBold" Margin="0,0,0,8" />
            <DataGrid Grid.Row="1" Grid.Column="2"
                      ItemsSource="{Binding Contracts}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="№" Binding="{Binding RegistrationNumber}" Width="120" />
                    <DataGridTextColumn Header="Контрагент" Binding="{Binding SupplierName}" Width="*" />
                    <DataGridTextColumn Header="Сумма, ₽" Binding="{Binding ContractAmount, StringFormat=N2}" Width="120" />
                    <DataGridTextColumn Header="Статус" Binding="{Binding ContractStatus}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,16,0,0">
            <TextBlock Text="Планов: " FontWeight="SemiBold" />
            <TextBlock Text="{Binding PlanCount}" Margin="0,0,16,0" />
            <TextBlock Text="Контрактов в исполнении: " FontWeight="SemiBold" />
            <TextBlock Text="{Binding ContractsInExecutionCount}" Margin="0,0,16,0" />
            <TextBlock Text="Сумма обязательств: " FontWeight="SemiBold" />
            <TextBlock Text="{Binding TotalCommittedAmount, StringFormat=N2}" Margin="0,0,16,0" />
            <TextBlock Text="Этапов в окне 30 дней: " FontWeight="SemiBold" />
            <TextBlock Text="{Binding UpcomingMilestones.Count}" />
        </StackPanel>

        <TextBlock Grid.Row="4" Text="{Binding StatusMessage}" Margin="0,12,0,0"
                   Foreground="#9CA3AF" />
    </Grid>
</UserControl>
