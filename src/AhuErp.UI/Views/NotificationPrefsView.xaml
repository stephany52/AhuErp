<UserControl x:Class="AhuErp.UI.Views.NotificationPrefsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AhuErp.UI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:NotificationPrefsViewModel}"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Настройки уведомлений"
                   FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12" />

        <TextBlock Grid.Row="1"
                   Text="Включите/выключите типы уведомлений и выберите канал доставки. Настройки применяются сразу после нажатия «Сохранить»."
                   Foreground="#6B7280" Margin="0,0,0,12" TextWrapping="Wrap" />

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Rows}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Тип уведомления"
                                    Binding="{Binding Kind, Converter={StaticResource EnumDisplayConverter}}"
                                    Width="*"
                                    IsReadOnly="True" />
                <DataGridCheckBoxColumn Header="Включено"
                                        Binding="{Binding IsEnabled, UpdateSourceTrigger=PropertyChanged}"
                                        Width="100" />
                <DataGridTemplateColumn Header="Канал доставки" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox SelectedItem="{Binding Channel, UpdateSourceTrigger=PropertyChanged}"
                                      ItemsSource="{Binding DataContext.AvailableChannels, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumDisplayConverter}}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Альтернативный e-mail"
                                    Binding="{Binding EmailOverride, UpdateSourceTrigger=PropertyChanged}"
                                    Width="240" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Padding="12,4" Margin="0,0,8,0" />
            <Button Content="Перезагрузить" Command="{Binding ReloadCommand}" Padding="12,4" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="#15803D" VerticalAlignment="Center" Margin="12,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
