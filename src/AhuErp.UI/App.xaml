<Application x:Class="AhuErp.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:AhuErp.UI.Converters"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <conv:StringToVisibilityConverter  x:Key="StringToVisibilityConverter" />
            <conv:OverdueRowColorConverter     x:Key="OverdueRowColorConverter" />
            <conv:EnumDisplayConverter         x:Key="EnumDisplayConverter" />
            <conv:AuditValuesConverter         x:Key="AuditValuesConverter" />
            <conv:BooleanDisplayConverter      x:Key="BooleanDisplayConverter" />
            <conv:InventoryDeltaSignConverter  x:Key="InventoryDeltaSignConverter" />

            <!-- Шаблон элемента ComboBox, отображающий enum через локализующий конвертер.
                 Используется через ItemTemplate="{StaticResource EnumComboBoxItemTemplate}"
                 у любого ComboBox, где ItemsSource — массив enum-значений. -->
            <DataTemplate x:Key="EnumComboBoxItemTemplate">
                <TextBlock Text="{Binding Converter={StaticResource EnumDisplayConverter}}" />
            </DataTemplate>

            <SolidColorBrush x:Key="Brush.Sidebar.Background" Color="#1F2A3A" />
            <SolidColorBrush x:Key="Brush.Sidebar.Foreground" Color="#E6ECF2" />
            <SolidColorBrush x:Key="Brush.Sidebar.Accent" Color="#3B82F6" />
            <SolidColorBrush x:Key="Brush.Content.Background" Color="#F5F7FA" />

            <Style TargetType="Button" x:Key="NavButton">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{StaticResource Brush.Sidebar.Foreground}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="Padding" Value="20,12" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#27344A" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#2F3E58" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
